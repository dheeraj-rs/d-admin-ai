import { ChatWorkspace } from '@/features/workspace';

export default function CodeEditorPage() {
    // This uses ChatWorkspace which handles the fullscreen-editor view state
    return <ChatWorkspace />;
}
